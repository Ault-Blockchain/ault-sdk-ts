// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/exchange/v1beta1/exchange.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DecCoin } from "../../../cosmos/base/v1beta1/coin";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "ault.exchange.v1beta1";

export interface Market {
  id: bigint;
  baseDenom: string;
  quoteDenom: string;
  escrowAddress: string;
  makerFeeRate: string;
  takerFeeRate: string;
}

export interface MarketState {
  lastPrice: string;
  lastMatchingHeight: bigint;
}

export interface Order {
  id: Uint8Array;
  orderer: string;
  marketId: bigint;
  isBuy: boolean;
  price: string;
  quantity: string;
  msgHeight: bigint;
  openQuantity: string;
  remainingDeposit: string;
  deadline: Date | undefined;
}

export interface SwapRouteResult {
  marketId: bigint;
  executedQuantity: string;
  input: DecCoin | undefined;
  output: DecCoin | undefined;
  fee: DecCoin | undefined;
}

function createBaseMarket(): Market {
  return { id: 0n, baseDenom: "", quoteDenom: "", escrowAddress: "", makerFeeRate: "", takerFeeRate: "" };
}

export const Market: MessageFns<Market> = {
  encode(message: Market, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.baseDenom !== "") {
      writer.uint32(18).string(message.baseDenom);
    }
    if (message.quoteDenom !== "") {
      writer.uint32(26).string(message.quoteDenom);
    }
    if (message.escrowAddress !== "") {
      writer.uint32(34).string(message.escrowAddress);
    }
    if (message.makerFeeRate !== "") {
      writer.uint32(42).string(message.makerFeeRate);
    }
    if (message.takerFeeRate !== "") {
      writer.uint32(50).string(message.takerFeeRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Market {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baseDenom = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quoteDenom = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.escrowAddress = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.makerFeeRate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.takerFeeRate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Market {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      baseDenom: isSet(object.baseDenom)
        ? globalThis.String(object.baseDenom)
        : isSet(object.base_denom)
        ? globalThis.String(object.base_denom)
        : "",
      quoteDenom: isSet(object.quoteDenom)
        ? globalThis.String(object.quoteDenom)
        : isSet(object.quote_denom)
        ? globalThis.String(object.quote_denom)
        : "",
      escrowAddress: isSet(object.escrowAddress)
        ? globalThis.String(object.escrowAddress)
        : isSet(object.escrow_address)
        ? globalThis.String(object.escrow_address)
        : "",
      makerFeeRate: isSet(object.makerFeeRate)
        ? globalThis.String(object.makerFeeRate)
        : isSet(object.maker_fee_rate)
        ? globalThis.String(object.maker_fee_rate)
        : "",
      takerFeeRate: isSet(object.takerFeeRate)
        ? globalThis.String(object.takerFeeRate)
        : isSet(object.taker_fee_rate)
        ? globalThis.String(object.taker_fee_rate)
        : "",
    };
  },

  toJSON(message: Market): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.baseDenom !== "") {
      obj.baseDenom = message.baseDenom;
    }
    if (message.quoteDenom !== "") {
      obj.quoteDenom = message.quoteDenom;
    }
    if (message.escrowAddress !== "") {
      obj.escrowAddress = message.escrowAddress;
    }
    if (message.makerFeeRate !== "") {
      obj.makerFeeRate = message.makerFeeRate;
    }
    if (message.takerFeeRate !== "") {
      obj.takerFeeRate = message.takerFeeRate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Market>, I>>(base?: I): Market {
    return Market.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Market>, I>>(object: I): Market {
    const message = createBaseMarket();
    message.id = object.id ?? 0n;
    message.baseDenom = object.baseDenom ?? "";
    message.quoteDenom = object.quoteDenom ?? "";
    message.escrowAddress = object.escrowAddress ?? "";
    message.makerFeeRate = object.makerFeeRate ?? "";
    message.takerFeeRate = object.takerFeeRate ?? "";
    return message;
  },
};

function createBaseMarketState(): MarketState {
  return { lastPrice: "", lastMatchingHeight: 0n };
}

export const MarketState: MessageFns<MarketState> = {
  encode(message: MarketState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lastPrice !== "") {
      writer.uint32(10).string(message.lastPrice);
    }
    if (message.lastMatchingHeight !== 0n) {
      if (BigInt.asIntN(64, message.lastMatchingHeight) !== message.lastMatchingHeight) {
        throw new globalThis.Error("value provided for field message.lastMatchingHeight of type int64 too large");
      }
      writer.uint32(16).int64(message.lastMatchingHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lastPrice = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lastMatchingHeight = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketState {
    return {
      lastPrice: isSet(object.lastPrice)
        ? globalThis.String(object.lastPrice)
        : isSet(object.last_price)
        ? globalThis.String(object.last_price)
        : "",
      lastMatchingHeight: isSet(object.lastMatchingHeight)
        ? BigInt(object.lastMatchingHeight)
        : isSet(object.last_matching_height)
        ? BigInt(object.last_matching_height)
        : 0n,
    };
  },

  toJSON(message: MarketState): unknown {
    const obj: any = {};
    if (message.lastPrice !== "") {
      obj.lastPrice = message.lastPrice;
    }
    if (message.lastMatchingHeight !== 0n) {
      obj.lastMatchingHeight = message.lastMatchingHeight.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketState>, I>>(base?: I): MarketState {
    return MarketState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketState>, I>>(object: I): MarketState {
    const message = createBaseMarketState();
    message.lastPrice = object.lastPrice ?? "";
    message.lastMatchingHeight = object.lastMatchingHeight ?? 0n;
    return message;
  },
};

function createBaseOrder(): Order {
  return {
    id: new Uint8Array(0),
    orderer: "",
    marketId: 0n,
    isBuy: false,
    price: "",
    quantity: "",
    msgHeight: 0n,
    openQuantity: "",
    remainingDeposit: "",
    deadline: undefined,
  };
}

export const Order: MessageFns<Order> = {
  encode(message: Order, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id.length !== 0) {
      writer.uint32(10).bytes(message.id);
    }
    if (message.orderer !== "") {
      writer.uint32(18).string(message.orderer);
    }
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(24).uint64(message.marketId);
    }
    if (message.isBuy !== false) {
      writer.uint32(32).bool(message.isBuy);
    }
    if (message.price !== "") {
      writer.uint32(42).string(message.price);
    }
    if (message.quantity !== "") {
      writer.uint32(50).string(message.quantity);
    }
    if (message.msgHeight !== 0n) {
      if (BigInt.asIntN(64, message.msgHeight) !== message.msgHeight) {
        throw new globalThis.Error("value provided for field message.msgHeight of type int64 too large");
      }
      writer.uint32(56).int64(message.msgHeight);
    }
    if (message.openQuantity !== "") {
      writer.uint32(66).string(message.openQuantity);
    }
    if (message.remainingDeposit !== "") {
      writer.uint32(74).string(message.remainingDeposit);
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Order {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderer = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isBuy = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.msgHeight = reader.int64() as bigint;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.openQuantity = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.remainingDeposit = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Order {
    return {
      id: isSet(object.id) ? bytesFromBase64(object.id) : new Uint8Array(0),
      orderer: isSet(object.orderer) ? globalThis.String(object.orderer) : "",
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      isBuy: isSet(object.isBuy)
        ? globalThis.Boolean(object.isBuy)
        : isSet(object.is_buy)
        ? globalThis.Boolean(object.is_buy)
        : false,
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
      msgHeight: isSet(object.msgHeight)
        ? BigInt(object.msgHeight)
        : isSet(object.msg_height)
        ? BigInt(object.msg_height)
        : 0n,
      openQuantity: isSet(object.openQuantity)
        ? globalThis.String(object.openQuantity)
        : isSet(object.open_quantity)
        ? globalThis.String(object.open_quantity)
        : "",
      remainingDeposit: isSet(object.remainingDeposit)
        ? globalThis.String(object.remainingDeposit)
        : isSet(object.remaining_deposit)
        ? globalThis.String(object.remaining_deposit)
        : "",
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
    };
  },

  toJSON(message: Order): unknown {
    const obj: any = {};
    if (message.id.length !== 0) {
      obj.id = base64FromBytes(message.id);
    }
    if (message.orderer !== "") {
      obj.orderer = message.orderer;
    }
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.isBuy !== false) {
      obj.isBuy = message.isBuy;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    if (message.msgHeight !== 0n) {
      obj.msgHeight = message.msgHeight.toString();
    }
    if (message.openQuantity !== "") {
      obj.openQuantity = message.openQuantity;
    }
    if (message.remainingDeposit !== "") {
      obj.remainingDeposit = message.remainingDeposit;
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Order>, I>>(base?: I): Order {
    return Order.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Order>, I>>(object: I): Order {
    const message = createBaseOrder();
    message.id = object.id ?? new Uint8Array(0);
    message.orderer = object.orderer ?? "";
    message.marketId = object.marketId ?? 0n;
    message.isBuy = object.isBuy ?? false;
    message.price = object.price ?? "";
    message.quantity = object.quantity ?? "";
    message.msgHeight = object.msgHeight ?? 0n;
    message.openQuantity = object.openQuantity ?? "";
    message.remainingDeposit = object.remainingDeposit ?? "";
    message.deadline = object.deadline ?? undefined;
    return message;
  },
};

function createBaseSwapRouteResult(): SwapRouteResult {
  return { marketId: 0n, executedQuantity: "", input: undefined, output: undefined, fee: undefined };
}

export const SwapRouteResult: MessageFns<SwapRouteResult> = {
  encode(message: SwapRouteResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    if (message.executedQuantity !== "") {
      writer.uint32(18).string(message.executedQuantity);
    }
    if (message.input !== undefined) {
      DecCoin.encode(message.input, writer.uint32(26).fork()).join();
    }
    if (message.output !== undefined) {
      DecCoin.encode(message.output, writer.uint32(34).fork()).join();
    }
    if (message.fee !== undefined) {
      DecCoin.encode(message.fee, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SwapRouteResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSwapRouteResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.executedQuantity = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.input = DecCoin.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.output = DecCoin.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fee = DecCoin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SwapRouteResult {
    return {
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      executedQuantity: isSet(object.executedQuantity)
        ? globalThis.String(object.executedQuantity)
        : isSet(object.executed_quantity)
        ? globalThis.String(object.executed_quantity)
        : "",
      input: isSet(object.input) ? DecCoin.fromJSON(object.input) : undefined,
      output: isSet(object.output) ? DecCoin.fromJSON(object.output) : undefined,
      fee: isSet(object.fee) ? DecCoin.fromJSON(object.fee) : undefined,
    };
  },

  toJSON(message: SwapRouteResult): unknown {
    const obj: any = {};
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.executedQuantity !== "") {
      obj.executedQuantity = message.executedQuantity;
    }
    if (message.input !== undefined) {
      obj.input = DecCoin.toJSON(message.input);
    }
    if (message.output !== undefined) {
      obj.output = DecCoin.toJSON(message.output);
    }
    if (message.fee !== undefined) {
      obj.fee = DecCoin.toJSON(message.fee);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SwapRouteResult>, I>>(base?: I): SwapRouteResult {
    return SwapRouteResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SwapRouteResult>, I>>(object: I): SwapRouteResult {
    const message = createBaseSwapRouteResult();
    message.marketId = object.marketId ?? 0n;
    message.executedQuantity = object.executedQuantity ?? "";
    message.input = (object.input !== undefined && object.input !== null)
      ? DecCoin.fromPartial(object.input)
      : undefined;
    message.output = (object.output !== undefined && object.output !== null)
      ? DecCoin.fromPartial(object.output)
      : undefined;
    message.fee = (object.fee !== undefined && object.fee !== null) ? DecCoin.fromPartial(object.fee) : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
