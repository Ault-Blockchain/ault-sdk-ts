// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/exchange/v1beta1/order_book.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "ault.exchange.v1beta1";

export interface OrderBook {
  priceInterval: string;
  sells: OrderBookPriceLevel[];
  buys: OrderBookPriceLevel[];
}

export interface OrderBookPriceLevel {
  /** p denotes price */
  p: string;
  /** q denotes quantity */
  q: string;
}

function createBaseOrderBook(): OrderBook {
  return { priceInterval: "", sells: [], buys: [] };
}

export const OrderBook: MessageFns<OrderBook> = {
  encode(message: OrderBook, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.priceInterval !== "") {
      writer.uint32(10).string(message.priceInterval);
    }
    for (const v of message.sells) {
      OrderBookPriceLevel.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.buys) {
      OrderBookPriceLevel.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderBook {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderBook();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.priceInterval = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sells.push(OrderBookPriceLevel.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.buys.push(OrderBookPriceLevel.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderBook {
    return {
      priceInterval: isSet(object.priceInterval)
        ? globalThis.String(object.priceInterval)
        : isSet(object.price_interval)
        ? globalThis.String(object.price_interval)
        : "",
      sells: globalThis.Array.isArray(object?.sells)
        ? object.sells.map((e: any) => OrderBookPriceLevel.fromJSON(e))
        : [],
      buys: globalThis.Array.isArray(object?.buys) ? object.buys.map((e: any) => OrderBookPriceLevel.fromJSON(e)) : [],
    };
  },

  toJSON(message: OrderBook): unknown {
    const obj: any = {};
    if (message.priceInterval !== "") {
      obj.priceInterval = message.priceInterval;
    }
    if (message.sells?.length) {
      obj.sells = message.sells.map((e) => OrderBookPriceLevel.toJSON(e));
    }
    if (message.buys?.length) {
      obj.buys = message.buys.map((e) => OrderBookPriceLevel.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OrderBook>, I>>(base?: I): OrderBook {
    return OrderBook.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OrderBook>, I>>(object: I): OrderBook {
    const message = createBaseOrderBook();
    message.priceInterval = object.priceInterval ?? "";
    message.sells = object.sells?.map((e) => OrderBookPriceLevel.fromPartial(e)) || [];
    message.buys = object.buys?.map((e) => OrderBookPriceLevel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOrderBookPriceLevel(): OrderBookPriceLevel {
  return { p: "", q: "" };
}

export const OrderBookPriceLevel: MessageFns<OrderBookPriceLevel> = {
  encode(message: OrderBookPriceLevel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.p !== "") {
      writer.uint32(10).string(message.p);
    }
    if (message.q !== "") {
      writer.uint32(18).string(message.q);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OrderBookPriceLevel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrderBookPriceLevel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.p = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.q = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OrderBookPriceLevel {
    return {
      p: isSet(object.p) ? globalThis.String(object.p) : "",
      q: isSet(object.q) ? globalThis.String(object.q) : "",
    };
  },

  toJSON(message: OrderBookPriceLevel): unknown {
    const obj: any = {};
    if (message.p !== "") {
      obj.p = message.p;
    }
    if (message.q !== "") {
      obj.q = message.q;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OrderBookPriceLevel>, I>>(base?: I): OrderBookPriceLevel {
    return OrderBookPriceLevel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OrderBookPriceLevel>, I>>(object: I): OrderBookPriceLevel {
    const message = createBaseOrderBookPriceLevel();
    message.p = object.p ?? "";
    message.q = object.q ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
