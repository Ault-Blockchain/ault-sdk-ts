// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/exchange/v1beta1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Market, MarketState, Order } from "./exchange";
import { Params } from "./params";

export const protobufPackage = "ault.exchange.v1beta1";

export interface GenesisState {
  params: Params | undefined;
  lastMarketId: bigint;
  marketRecords: MarketRecord[];
  orders: Order[];
}

export interface MarketRecord {
  market: Market | undefined;
  state: MarketState | undefined;
}

function createBaseGenesisState(): GenesisState {
  return { params: undefined, lastMarketId: 0n, marketRecords: [], orders: [] };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    if (message.lastMarketId !== 0n) {
      if (BigInt.asUintN(64, message.lastMarketId) !== message.lastMarketId) {
        throw new globalThis.Error("value provided for field message.lastMarketId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.lastMarketId);
    }
    for (const v of message.marketRecords) {
      MarketRecord.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.orders) {
      Order.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.lastMarketId = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.marketRecords.push(MarketRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.orders.push(Order.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      lastMarketId: isSet(object.lastMarketId)
        ? BigInt(object.lastMarketId)
        : isSet(object.last_market_id)
        ? BigInt(object.last_market_id)
        : 0n,
      marketRecords: globalThis.Array.isArray(object?.marketRecords)
        ? object.marketRecords.map((e: any) => MarketRecord.fromJSON(e))
        : globalThis.Array.isArray(object?.market_records)
        ? object.market_records.map((e: any) => MarketRecord.fromJSON(e))
        : [],
      orders: globalThis.Array.isArray(object?.orders) ? object.orders.map((e: any) => Order.fromJSON(e)) : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.lastMarketId !== 0n) {
      obj.lastMarketId = message.lastMarketId.toString();
    }
    if (message.marketRecords?.length) {
      obj.marketRecords = message.marketRecords.map((e) => MarketRecord.toJSON(e));
    }
    if (message.orders?.length) {
      obj.orders = message.orders.map((e) => Order.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.lastMarketId = object.lastMarketId ?? 0n;
    message.marketRecords = object.marketRecords?.map((e) => MarketRecord.fromPartial(e)) || [];
    message.orders = object.orders?.map((e) => Order.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMarketRecord(): MarketRecord {
  return { market: undefined, state: undefined };
}

export const MarketRecord: MessageFns<MarketRecord> = {
  encode(message: MarketRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.market !== undefined) {
      Market.encode(message.market, writer.uint32(10).fork()).join();
    }
    if (message.state !== undefined) {
      MarketState.encode(message.state, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.market = Market.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = MarketState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketRecord {
    return {
      market: isSet(object.market) ? Market.fromJSON(object.market) : undefined,
      state: isSet(object.state) ? MarketState.fromJSON(object.state) : undefined,
    };
  },

  toJSON(message: MarketRecord): unknown {
    const obj: any = {};
    if (message.market !== undefined) {
      obj.market = Market.toJSON(message.market);
    }
    if (message.state !== undefined) {
      obj.state = MarketState.toJSON(message.state);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketRecord>, I>>(base?: I): MarketRecord {
    return MarketRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketRecord>, I>>(object: I): MarketRecord {
    const message = createBaseMarketRecord();
    message.market = (object.market !== undefined && object.market !== null)
      ? Market.fromPartial(object.market)
      : undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? MarketState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
