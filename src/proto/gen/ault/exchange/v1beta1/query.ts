// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/exchange/v1beta1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination";
import { Order } from "./exchange";
import { OrderBook } from "./order_book";
import { Params } from "./params";

export const protobufPackage = "ault.exchange.v1beta1";

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params: Params | undefined;
}

export interface QueryAllMarketsRequest {
  pagination: PageRequest | undefined;
}

export interface QueryAllMarketsResponse {
  markets: MarketResponse[];
  pagination: PageResponse | undefined;
}

export interface QueryMarketRequest {
  marketId: bigint;
}

export interface QueryMarketResponse {
  market: MarketResponse | undefined;
}

export interface QueryAllOrdersRequest {
  orderer: string;
  marketId: bigint;
  pagination: PageRequest | undefined;
}

export interface QueryAllOrdersResponse {
  orders: Order[];
  pagination: PageResponse | undefined;
}

export interface QueryOrderRequest {
  orderId: Uint8Array;
}

export interface QueryOrderResponse {
  order: Order | undefined;
}

export interface QueryOrderBookRequest {
  marketId: bigint;
  /** Optional: start price level (inclusive) */
  levelStart: number;
  /** Optional: end price level (inclusive) */
  levelEnd: number;
}

export interface QueryOrderBookResponse {
  orderBooks: OrderBook[];
}

export interface MarketResponse {
  id: bigint;
  baseDenom: string;
  quoteDenom: string;
  escrowAddress: string;
  makerFeeRate: string;
  takerFeeRate: string;
  lastPrice: string;
  lastMatchingHeight: bigint;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryAllMarketsRequest(): QueryAllMarketsRequest {
  return { pagination: undefined };
}

export const QueryAllMarketsRequest: MessageFns<QueryAllMarketsRequest> = {
  encode(message: QueryAllMarketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAllMarketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllMarketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllMarketsRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryAllMarketsRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAllMarketsRequest>, I>>(base?: I): QueryAllMarketsRequest {
    return QueryAllMarketsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAllMarketsRequest>, I>>(object: I): QueryAllMarketsRequest {
    const message = createBaseQueryAllMarketsRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryAllMarketsResponse(): QueryAllMarketsResponse {
  return { markets: [], pagination: undefined };
}

export const QueryAllMarketsResponse: MessageFns<QueryAllMarketsResponse> = {
  encode(message: QueryAllMarketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.markets) {
      MarketResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAllMarketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllMarketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.markets.push(MarketResponse.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllMarketsResponse {
    return {
      markets: globalThis.Array.isArray(object?.markets)
        ? object.markets.map((e: any) => MarketResponse.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryAllMarketsResponse): unknown {
    const obj: any = {};
    if (message.markets?.length) {
      obj.markets = message.markets.map((e) => MarketResponse.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAllMarketsResponse>, I>>(base?: I): QueryAllMarketsResponse {
    return QueryAllMarketsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAllMarketsResponse>, I>>(object: I): QueryAllMarketsResponse {
    const message = createBaseQueryAllMarketsResponse();
    message.markets = object.markets?.map((e) => MarketResponse.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryMarketRequest(): QueryMarketRequest {
  return { marketId: 0n };
}

export const QueryMarketRequest: MessageFns<QueryMarketRequest> = {
  encode(message: QueryMarketRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMarketRequest {
    return {
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
    };
  },

  toJSON(message: QueryMarketRequest): unknown {
    const obj: any = {};
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryMarketRequest>, I>>(base?: I): QueryMarketRequest {
    return QueryMarketRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketRequest>, I>>(object: I): QueryMarketRequest {
    const message = createBaseQueryMarketRequest();
    message.marketId = object.marketId ?? 0n;
    return message;
  },
};

function createBaseQueryMarketResponse(): QueryMarketResponse {
  return { market: undefined };
}

export const QueryMarketResponse: MessageFns<QueryMarketResponse> = {
  encode(message: QueryMarketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.market !== undefined) {
      MarketResponse.encode(message.market, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.market = MarketResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMarketResponse {
    return { market: isSet(object.market) ? MarketResponse.fromJSON(object.market) : undefined };
  },

  toJSON(message: QueryMarketResponse): unknown {
    const obj: any = {};
    if (message.market !== undefined) {
      obj.market = MarketResponse.toJSON(message.market);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryMarketResponse>, I>>(base?: I): QueryMarketResponse {
    return QueryMarketResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketResponse>, I>>(object: I): QueryMarketResponse {
    const message = createBaseQueryMarketResponse();
    message.market = (object.market !== undefined && object.market !== null)
      ? MarketResponse.fromPartial(object.market)
      : undefined;
    return message;
  },
};

function createBaseQueryAllOrdersRequest(): QueryAllOrdersRequest {
  return { orderer: "", marketId: 0n, pagination: undefined };
}

export const QueryAllOrdersRequest: MessageFns<QueryAllOrdersRequest> = {
  encode(message: QueryAllOrdersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderer !== "") {
      writer.uint32(10).string(message.orderer);
    }
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.marketId);
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAllOrdersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllOrdersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllOrdersRequest {
    return {
      orderer: isSet(object.orderer) ? globalThis.String(object.orderer) : "",
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryAllOrdersRequest): unknown {
    const obj: any = {};
    if (message.orderer !== "") {
      obj.orderer = message.orderer;
    }
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAllOrdersRequest>, I>>(base?: I): QueryAllOrdersRequest {
    return QueryAllOrdersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAllOrdersRequest>, I>>(object: I): QueryAllOrdersRequest {
    const message = createBaseQueryAllOrdersRequest();
    message.orderer = object.orderer ?? "";
    message.marketId = object.marketId ?? 0n;
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryAllOrdersResponse(): QueryAllOrdersResponse {
  return { orders: [], pagination: undefined };
}

export const QueryAllOrdersResponse: MessageFns<QueryAllOrdersResponse> = {
  encode(message: QueryAllOrdersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.orders) {
      Order.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAllOrdersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllOrdersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orders.push(Order.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllOrdersResponse {
    return {
      orders: globalThis.Array.isArray(object?.orders) ? object.orders.map((e: any) => Order.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryAllOrdersResponse): unknown {
    const obj: any = {};
    if (message.orders?.length) {
      obj.orders = message.orders.map((e) => Order.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAllOrdersResponse>, I>>(base?: I): QueryAllOrdersResponse {
    return QueryAllOrdersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAllOrdersResponse>, I>>(object: I): QueryAllOrdersResponse {
    const message = createBaseQueryAllOrdersResponse();
    message.orders = object.orders?.map((e) => Order.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryOrderRequest(): QueryOrderRequest {
  return { orderId: new Uint8Array(0) };
}

export const QueryOrderRequest: MessageFns<QueryOrderRequest> = {
  encode(message: QueryOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId.length !== 0) {
      writer.uint32(10).bytes(message.orderId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryOrderRequest {
    return {
      orderId: isSet(object.orderId)
        ? bytesFromBase64(object.orderId)
        : isSet(object.order_id)
        ? bytesFromBase64(object.order_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: QueryOrderRequest): unknown {
    const obj: any = {};
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryOrderRequest>, I>>(base?: I): QueryOrderRequest {
    return QueryOrderRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryOrderRequest>, I>>(object: I): QueryOrderRequest {
    const message = createBaseQueryOrderRequest();
    message.orderId = object.orderId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryOrderResponse(): QueryOrderResponse {
  return { order: undefined };
}

export const QueryOrderResponse: MessageFns<QueryOrderResponse> = {
  encode(message: QueryOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.order !== undefined) {
      Order.encode(message.order, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.order = Order.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryOrderResponse {
    return { order: isSet(object.order) ? Order.fromJSON(object.order) : undefined };
  },

  toJSON(message: QueryOrderResponse): unknown {
    const obj: any = {};
    if (message.order !== undefined) {
      obj.order = Order.toJSON(message.order);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryOrderResponse>, I>>(base?: I): QueryOrderResponse {
    return QueryOrderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryOrderResponse>, I>>(object: I): QueryOrderResponse {
    const message = createBaseQueryOrderResponse();
    message.order = (object.order !== undefined && object.order !== null) ? Order.fromPartial(object.order) : undefined;
    return message;
  },
};

function createBaseQueryOrderBookRequest(): QueryOrderBookRequest {
  return { marketId: 0n, levelStart: 0, levelEnd: 0 };
}

export const QueryOrderBookRequest: MessageFns<QueryOrderBookRequest> = {
  encode(message: QueryOrderBookRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    if (message.levelStart !== 0) {
      writer.uint32(16).int32(message.levelStart);
    }
    if (message.levelEnd !== 0) {
      writer.uint32(24).int32(message.levelEnd);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryOrderBookRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOrderBookRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.levelStart = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.levelEnd = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryOrderBookRequest {
    return {
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      levelStart: isSet(object.levelStart)
        ? globalThis.Number(object.levelStart)
        : isSet(object.level_start)
        ? globalThis.Number(object.level_start)
        : 0,
      levelEnd: isSet(object.levelEnd)
        ? globalThis.Number(object.levelEnd)
        : isSet(object.level_end)
        ? globalThis.Number(object.level_end)
        : 0,
    };
  },

  toJSON(message: QueryOrderBookRequest): unknown {
    const obj: any = {};
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.levelStart !== 0) {
      obj.levelStart = Math.round(message.levelStart);
    }
    if (message.levelEnd !== 0) {
      obj.levelEnd = Math.round(message.levelEnd);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryOrderBookRequest>, I>>(base?: I): QueryOrderBookRequest {
    return QueryOrderBookRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryOrderBookRequest>, I>>(object: I): QueryOrderBookRequest {
    const message = createBaseQueryOrderBookRequest();
    message.marketId = object.marketId ?? 0n;
    message.levelStart = object.levelStart ?? 0;
    message.levelEnd = object.levelEnd ?? 0;
    return message;
  },
};

function createBaseQueryOrderBookResponse(): QueryOrderBookResponse {
  return { orderBooks: [] };
}

export const QueryOrderBookResponse: MessageFns<QueryOrderBookResponse> = {
  encode(message: QueryOrderBookResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.orderBooks) {
      OrderBook.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryOrderBookResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOrderBookResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderBooks.push(OrderBook.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryOrderBookResponse {
    return {
      orderBooks: globalThis.Array.isArray(object?.orderBooks)
        ? object.orderBooks.map((e: any) => OrderBook.fromJSON(e))
        : globalThis.Array.isArray(object?.order_books)
        ? object.order_books.map((e: any) => OrderBook.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryOrderBookResponse): unknown {
    const obj: any = {};
    if (message.orderBooks?.length) {
      obj.orderBooks = message.orderBooks.map((e) => OrderBook.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryOrderBookResponse>, I>>(base?: I): QueryOrderBookResponse {
    return QueryOrderBookResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryOrderBookResponse>, I>>(object: I): QueryOrderBookResponse {
    const message = createBaseQueryOrderBookResponse();
    message.orderBooks = object.orderBooks?.map((e) => OrderBook.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMarketResponse(): MarketResponse {
  return {
    id: 0n,
    baseDenom: "",
    quoteDenom: "",
    escrowAddress: "",
    makerFeeRate: "",
    takerFeeRate: "",
    lastPrice: "",
    lastMatchingHeight: 0n,
  };
}

export const MarketResponse: MessageFns<MarketResponse> = {
  encode(message: MarketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.baseDenom !== "") {
      writer.uint32(18).string(message.baseDenom);
    }
    if (message.quoteDenom !== "") {
      writer.uint32(26).string(message.quoteDenom);
    }
    if (message.escrowAddress !== "") {
      writer.uint32(34).string(message.escrowAddress);
    }
    if (message.makerFeeRate !== "") {
      writer.uint32(42).string(message.makerFeeRate);
    }
    if (message.takerFeeRate !== "") {
      writer.uint32(50).string(message.takerFeeRate);
    }
    if (message.lastPrice !== "") {
      writer.uint32(58).string(message.lastPrice);
    }
    if (message.lastMatchingHeight !== 0n) {
      if (BigInt.asIntN(64, message.lastMatchingHeight) !== message.lastMatchingHeight) {
        throw new globalThis.Error("value provided for field message.lastMatchingHeight of type int64 too large");
      }
      writer.uint32(64).int64(message.lastMatchingHeight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baseDenom = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quoteDenom = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.escrowAddress = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.makerFeeRate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.takerFeeRate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastPrice = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.lastMatchingHeight = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketResponse {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      baseDenom: isSet(object.baseDenom)
        ? globalThis.String(object.baseDenom)
        : isSet(object.base_denom)
        ? globalThis.String(object.base_denom)
        : "",
      quoteDenom: isSet(object.quoteDenom)
        ? globalThis.String(object.quoteDenom)
        : isSet(object.quote_denom)
        ? globalThis.String(object.quote_denom)
        : "",
      escrowAddress: isSet(object.escrowAddress)
        ? globalThis.String(object.escrowAddress)
        : isSet(object.escrow_address)
        ? globalThis.String(object.escrow_address)
        : "",
      makerFeeRate: isSet(object.makerFeeRate)
        ? globalThis.String(object.makerFeeRate)
        : isSet(object.maker_fee_rate)
        ? globalThis.String(object.maker_fee_rate)
        : "",
      takerFeeRate: isSet(object.takerFeeRate)
        ? globalThis.String(object.takerFeeRate)
        : isSet(object.taker_fee_rate)
        ? globalThis.String(object.taker_fee_rate)
        : "",
      lastPrice: isSet(object.lastPrice)
        ? globalThis.String(object.lastPrice)
        : isSet(object.last_price)
        ? globalThis.String(object.last_price)
        : "",
      lastMatchingHeight: isSet(object.lastMatchingHeight)
        ? BigInt(object.lastMatchingHeight)
        : isSet(object.last_matching_height)
        ? BigInt(object.last_matching_height)
        : 0n,
    };
  },

  toJSON(message: MarketResponse): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.baseDenom !== "") {
      obj.baseDenom = message.baseDenom;
    }
    if (message.quoteDenom !== "") {
      obj.quoteDenom = message.quoteDenom;
    }
    if (message.escrowAddress !== "") {
      obj.escrowAddress = message.escrowAddress;
    }
    if (message.makerFeeRate !== "") {
      obj.makerFeeRate = message.makerFeeRate;
    }
    if (message.takerFeeRate !== "") {
      obj.takerFeeRate = message.takerFeeRate;
    }
    if (message.lastPrice !== "") {
      obj.lastPrice = message.lastPrice;
    }
    if (message.lastMatchingHeight !== 0n) {
      obj.lastMatchingHeight = message.lastMatchingHeight.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketResponse>, I>>(base?: I): MarketResponse {
    return MarketResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketResponse>, I>>(object: I): MarketResponse {
    const message = createBaseMarketResponse();
    message.id = object.id ?? 0n;
    message.baseDenom = object.baseDenom ?? "";
    message.quoteDenom = object.quoteDenom ?? "";
    message.escrowAddress = object.escrowAddress ?? "";
    message.makerFeeRate = object.makerFeeRate ?? "";
    message.takerFeeRate = object.takerFeeRate ?? "";
    message.lastPrice = object.lastPrice ?? "";
    message.lastMatchingHeight = object.lastMatchingHeight ?? 0n;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
