// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/exchange/v1beta1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin";
import { Duration } from "../../../google/protobuf/duration";

export const protobufPackage = "ault.exchange.v1beta1";

export interface MsgCreateMarket {
  sender: string;
  baseDenom: string;
  quoteDenom: string;
}

export interface MsgCreateMarketResponse {
  marketId: bigint;
}

export interface MsgPlaceLimitOrder {
  sender: string;
  marketId: bigint;
  isBuy: boolean;
  price: string;
  quantity: string;
  lifespan: Duration | undefined;
}

export interface MsgPlaceLimitOrderResponse {
  orderId: Uint8Array;
  escrowedAmount: Coin | undefined;
}

export interface MsgPlaceMarketOrder {
  sender: string;
  marketId: bigint;
  isBuy: boolean;
  quantity: string;
}

export interface MsgPlaceMarketOrderResponse {
  orderId: Uint8Array;
  escrowedAmount: Coin | undefined;
}

export interface MsgCancelOrder {
  sender: string;
  orderId: Uint8Array;
}

export interface MsgCancelOrderResponse {
}

export interface MsgCancelAllOrders {
  sender: string;
  marketId: bigint;
}

export interface MsgCancelAllOrdersResponse {
  cancelledOrderIds: Uint8Array[];
}

export interface MarketParamUpdate {
  marketId: bigint;
  makerFeeRate: string;
  takerFeeRate: string;
}

export interface MsgUpdateMarketParams {
  /** authority is the address that controls the module (typically x/gov) */
  authority: string;
  /** updates contains the market parameter changes */
  updates: MarketParamUpdate[];
}

export interface MsgUpdateMarketParamsResponse {
}

function createBaseMsgCreateMarket(): MsgCreateMarket {
  return { sender: "", baseDenom: "", quoteDenom: "" };
}

export const MsgCreateMarket: MessageFns<MsgCreateMarket> = {
  encode(message: MsgCreateMarket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.baseDenom !== "") {
      writer.uint32(18).string(message.baseDenom);
    }
    if (message.quoteDenom !== "") {
      writer.uint32(26).string(message.quoteDenom);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateMarket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateMarket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baseDenom = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.quoteDenom = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateMarket {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      baseDenom: isSet(object.baseDenom)
        ? globalThis.String(object.baseDenom)
        : isSet(object.base_denom)
        ? globalThis.String(object.base_denom)
        : "",
      quoteDenom: isSet(object.quoteDenom)
        ? globalThis.String(object.quoteDenom)
        : isSet(object.quote_denom)
        ? globalThis.String(object.quote_denom)
        : "",
    };
  },

  toJSON(message: MsgCreateMarket): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.baseDenom !== "") {
      obj.baseDenom = message.baseDenom;
    }
    if (message.quoteDenom !== "") {
      obj.quoteDenom = message.quoteDenom;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreateMarket>, I>>(base?: I): MsgCreateMarket {
    return MsgCreateMarket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreateMarket>, I>>(object: I): MsgCreateMarket {
    const message = createBaseMsgCreateMarket();
    message.sender = object.sender ?? "";
    message.baseDenom = object.baseDenom ?? "";
    message.quoteDenom = object.quoteDenom ?? "";
    return message;
  },
};

function createBaseMsgCreateMarketResponse(): MsgCreateMarketResponse {
  return { marketId: 0n };
}

export const MsgCreateMarketResponse: MessageFns<MsgCreateMarketResponse> = {
  encode(message: MsgCreateMarketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateMarketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateMarketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateMarketResponse {
    return {
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
    };
  },

  toJSON(message: MsgCreateMarketResponse): unknown {
    const obj: any = {};
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreateMarketResponse>, I>>(base?: I): MsgCreateMarketResponse {
    return MsgCreateMarketResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreateMarketResponse>, I>>(object: I): MsgCreateMarketResponse {
    const message = createBaseMsgCreateMarketResponse();
    message.marketId = object.marketId ?? 0n;
    return message;
  },
};

function createBaseMsgPlaceLimitOrder(): MsgPlaceLimitOrder {
  return { sender: "", marketId: 0n, isBuy: false, price: "", quantity: "", lifespan: undefined };
}

export const MsgPlaceLimitOrder: MessageFns<MsgPlaceLimitOrder> = {
  encode(message: MsgPlaceLimitOrder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.marketId);
    }
    if (message.isBuy !== false) {
      writer.uint32(24).bool(message.isBuy);
    }
    if (message.price !== "") {
      writer.uint32(34).string(message.price);
    }
    if (message.quantity !== "") {
      writer.uint32(42).string(message.quantity);
    }
    if (message.lifespan !== undefined) {
      Duration.encode(message.lifespan, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPlaceLimitOrder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPlaceLimitOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isBuy = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.lifespan = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPlaceLimitOrder {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      isBuy: isSet(object.isBuy)
        ? globalThis.Boolean(object.isBuy)
        : isSet(object.is_buy)
        ? globalThis.Boolean(object.is_buy)
        : false,
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
      lifespan: isSet(object.lifespan) ? Duration.fromJSON(object.lifespan) : undefined,
    };
  },

  toJSON(message: MsgPlaceLimitOrder): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.isBuy !== false) {
      obj.isBuy = message.isBuy;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    if (message.lifespan !== undefined) {
      obj.lifespan = Duration.toJSON(message.lifespan);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPlaceLimitOrder>, I>>(base?: I): MsgPlaceLimitOrder {
    return MsgPlaceLimitOrder.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPlaceLimitOrder>, I>>(object: I): MsgPlaceLimitOrder {
    const message = createBaseMsgPlaceLimitOrder();
    message.sender = object.sender ?? "";
    message.marketId = object.marketId ?? 0n;
    message.isBuy = object.isBuy ?? false;
    message.price = object.price ?? "";
    message.quantity = object.quantity ?? "";
    message.lifespan = (object.lifespan !== undefined && object.lifespan !== null)
      ? Duration.fromPartial(object.lifespan)
      : undefined;
    return message;
  },
};

function createBaseMsgPlaceLimitOrderResponse(): MsgPlaceLimitOrderResponse {
  return { orderId: new Uint8Array(0), escrowedAmount: undefined };
}

export const MsgPlaceLimitOrderResponse: MessageFns<MsgPlaceLimitOrderResponse> = {
  encode(message: MsgPlaceLimitOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId.length !== 0) {
      writer.uint32(10).bytes(message.orderId);
    }
    if (message.escrowedAmount !== undefined) {
      Coin.encode(message.escrowedAmount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPlaceLimitOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPlaceLimitOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.escrowedAmount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPlaceLimitOrderResponse {
    return {
      orderId: isSet(object.orderId)
        ? bytesFromBase64(object.orderId)
        : isSet(object.order_id)
        ? bytesFromBase64(object.order_id)
        : new Uint8Array(0),
      escrowedAmount: isSet(object.escrowedAmount)
        ? Coin.fromJSON(object.escrowedAmount)
        : isSet(object.escrowed_amount)
        ? Coin.fromJSON(object.escrowed_amount)
        : undefined,
    };
  },

  toJSON(message: MsgPlaceLimitOrderResponse): unknown {
    const obj: any = {};
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    if (message.escrowedAmount !== undefined) {
      obj.escrowedAmount = Coin.toJSON(message.escrowedAmount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPlaceLimitOrderResponse>, I>>(base?: I): MsgPlaceLimitOrderResponse {
    return MsgPlaceLimitOrderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPlaceLimitOrderResponse>, I>>(object: I): MsgPlaceLimitOrderResponse {
    const message = createBaseMsgPlaceLimitOrderResponse();
    message.orderId = object.orderId ?? new Uint8Array(0);
    message.escrowedAmount = (object.escrowedAmount !== undefined && object.escrowedAmount !== null)
      ? Coin.fromPartial(object.escrowedAmount)
      : undefined;
    return message;
  },
};

function createBaseMsgPlaceMarketOrder(): MsgPlaceMarketOrder {
  return { sender: "", marketId: 0n, isBuy: false, quantity: "" };
}

export const MsgPlaceMarketOrder: MessageFns<MsgPlaceMarketOrder> = {
  encode(message: MsgPlaceMarketOrder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.marketId);
    }
    if (message.isBuy !== false) {
      writer.uint32(24).bool(message.isBuy);
    }
    if (message.quantity !== "") {
      writer.uint32(34).string(message.quantity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPlaceMarketOrder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPlaceMarketOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isBuy = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPlaceMarketOrder {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      isBuy: isSet(object.isBuy)
        ? globalThis.Boolean(object.isBuy)
        : isSet(object.is_buy)
        ? globalThis.Boolean(object.is_buy)
        : false,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
    };
  },

  toJSON(message: MsgPlaceMarketOrder): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.isBuy !== false) {
      obj.isBuy = message.isBuy;
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPlaceMarketOrder>, I>>(base?: I): MsgPlaceMarketOrder {
    return MsgPlaceMarketOrder.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPlaceMarketOrder>, I>>(object: I): MsgPlaceMarketOrder {
    const message = createBaseMsgPlaceMarketOrder();
    message.sender = object.sender ?? "";
    message.marketId = object.marketId ?? 0n;
    message.isBuy = object.isBuy ?? false;
    message.quantity = object.quantity ?? "";
    return message;
  },
};

function createBaseMsgPlaceMarketOrderResponse(): MsgPlaceMarketOrderResponse {
  return { orderId: new Uint8Array(0), escrowedAmount: undefined };
}

export const MsgPlaceMarketOrderResponse: MessageFns<MsgPlaceMarketOrderResponse> = {
  encode(message: MsgPlaceMarketOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.orderId.length !== 0) {
      writer.uint32(10).bytes(message.orderId);
    }
    if (message.escrowedAmount !== undefined) {
      Coin.encode(message.escrowedAmount, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPlaceMarketOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPlaceMarketOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orderId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.escrowedAmount = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPlaceMarketOrderResponse {
    return {
      orderId: isSet(object.orderId)
        ? bytesFromBase64(object.orderId)
        : isSet(object.order_id)
        ? bytesFromBase64(object.order_id)
        : new Uint8Array(0),
      escrowedAmount: isSet(object.escrowedAmount)
        ? Coin.fromJSON(object.escrowedAmount)
        : isSet(object.escrowed_amount)
        ? Coin.fromJSON(object.escrowed_amount)
        : undefined,
    };
  },

  toJSON(message: MsgPlaceMarketOrderResponse): unknown {
    const obj: any = {};
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    if (message.escrowedAmount !== undefined) {
      obj.escrowedAmount = Coin.toJSON(message.escrowedAmount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgPlaceMarketOrderResponse>, I>>(base?: I): MsgPlaceMarketOrderResponse {
    return MsgPlaceMarketOrderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgPlaceMarketOrderResponse>, I>>(object: I): MsgPlaceMarketOrderResponse {
    const message = createBaseMsgPlaceMarketOrderResponse();
    message.orderId = object.orderId ?? new Uint8Array(0);
    message.escrowedAmount = (object.escrowedAmount !== undefined && object.escrowedAmount !== null)
      ? Coin.fromPartial(object.escrowedAmount)
      : undefined;
    return message;
  },
};

function createBaseMsgCancelOrder(): MsgCancelOrder {
  return { sender: "", orderId: new Uint8Array(0) };
}

export const MsgCancelOrder: MessageFns<MsgCancelOrder> = {
  encode(message: MsgCancelOrder, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.orderId.length !== 0) {
      writer.uint32(18).bytes(message.orderId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCancelOrder {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCancelOrder {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      orderId: isSet(object.orderId)
        ? bytesFromBase64(object.orderId)
        : isSet(object.order_id)
        ? bytesFromBase64(object.order_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgCancelOrder): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCancelOrder>, I>>(base?: I): MsgCancelOrder {
    return MsgCancelOrder.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCancelOrder>, I>>(object: I): MsgCancelOrder {
    const message = createBaseMsgCancelOrder();
    message.sender = object.sender ?? "";
    message.orderId = object.orderId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgCancelOrderResponse(): MsgCancelOrderResponse {
  return {};
}

export const MsgCancelOrderResponse: MessageFns<MsgCancelOrderResponse> = {
  encode(_: MsgCancelOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCancelOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgCancelOrderResponse {
    return {};
  },

  toJSON(_: MsgCancelOrderResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCancelOrderResponse>, I>>(base?: I): MsgCancelOrderResponse {
    return MsgCancelOrderResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCancelOrderResponse>, I>>(_: I): MsgCancelOrderResponse {
    const message = createBaseMsgCancelOrderResponse();
    return message;
  },
};

function createBaseMsgCancelAllOrders(): MsgCancelAllOrders {
  return { sender: "", marketId: 0n };
}

export const MsgCancelAllOrders: MessageFns<MsgCancelAllOrders> = {
  encode(message: MsgCancelAllOrders, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.marketId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCancelAllOrders {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelAllOrders();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCancelAllOrders {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
    };
  },

  toJSON(message: MsgCancelAllOrders): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCancelAllOrders>, I>>(base?: I): MsgCancelAllOrders {
    return MsgCancelAllOrders.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCancelAllOrders>, I>>(object: I): MsgCancelAllOrders {
    const message = createBaseMsgCancelAllOrders();
    message.sender = object.sender ?? "";
    message.marketId = object.marketId ?? 0n;
    return message;
  },
};

function createBaseMsgCancelAllOrdersResponse(): MsgCancelAllOrdersResponse {
  return { cancelledOrderIds: [] };
}

export const MsgCancelAllOrdersResponse: MessageFns<MsgCancelAllOrdersResponse> = {
  encode(message: MsgCancelAllOrdersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.cancelledOrderIds) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCancelAllOrdersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelAllOrdersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cancelledOrderIds.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCancelAllOrdersResponse {
    return {
      cancelledOrderIds: globalThis.Array.isArray(object?.cancelledOrderIds)
        ? object.cancelledOrderIds.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.cancelled_order_ids)
        ? object.cancelled_order_ids.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: MsgCancelAllOrdersResponse): unknown {
    const obj: any = {};
    if (message.cancelledOrderIds?.length) {
      obj.cancelledOrderIds = message.cancelledOrderIds.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCancelAllOrdersResponse>, I>>(base?: I): MsgCancelAllOrdersResponse {
    return MsgCancelAllOrdersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCancelAllOrdersResponse>, I>>(object: I): MsgCancelAllOrdersResponse {
    const message = createBaseMsgCancelAllOrdersResponse();
    message.cancelledOrderIds = object.cancelledOrderIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseMarketParamUpdate(): MarketParamUpdate {
  return { marketId: 0n, makerFeeRate: "", takerFeeRate: "" };
}

export const MarketParamUpdate: MessageFns<MarketParamUpdate> = {
  encode(message: MarketParamUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    if (message.makerFeeRate !== "") {
      writer.uint32(18).string(message.makerFeeRate);
    }
    if (message.takerFeeRate !== "") {
      writer.uint32(26).string(message.takerFeeRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketParamUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketParamUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.makerFeeRate = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.takerFeeRate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MarketParamUpdate {
    return {
      marketId: isSet(object.marketId)
        ? BigInt(object.marketId)
        : isSet(object.market_id)
        ? BigInt(object.market_id)
        : 0n,
      makerFeeRate: isSet(object.makerFeeRate)
        ? globalThis.String(object.makerFeeRate)
        : isSet(object.maker_fee_rate)
        ? globalThis.String(object.maker_fee_rate)
        : "",
      takerFeeRate: isSet(object.takerFeeRate)
        ? globalThis.String(object.takerFeeRate)
        : isSet(object.taker_fee_rate)
        ? globalThis.String(object.taker_fee_rate)
        : "",
    };
  },

  toJSON(message: MarketParamUpdate): unknown {
    const obj: any = {};
    if (message.marketId !== 0n) {
      obj.marketId = message.marketId.toString();
    }
    if (message.makerFeeRate !== "") {
      obj.makerFeeRate = message.makerFeeRate;
    }
    if (message.takerFeeRate !== "") {
      obj.takerFeeRate = message.takerFeeRate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MarketParamUpdate>, I>>(base?: I): MarketParamUpdate {
    return MarketParamUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketParamUpdate>, I>>(object: I): MarketParamUpdate {
    const message = createBaseMarketParamUpdate();
    message.marketId = object.marketId ?? 0n;
    message.makerFeeRate = object.makerFeeRate ?? "";
    message.takerFeeRate = object.takerFeeRate ?? "";
    return message;
  },
};

function createBaseMsgUpdateMarketParams(): MsgUpdateMarketParams {
  return { authority: "", updates: [] };
}

export const MsgUpdateMarketParams: MessageFns<MsgUpdateMarketParams> = {
  encode(message: MsgUpdateMarketParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    for (const v of message.updates) {
      MarketParamUpdate.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateMarketParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateMarketParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.updates.push(MarketParamUpdate.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateMarketParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      updates: globalThis.Array.isArray(object?.updates)
        ? object.updates.map((e: any) => MarketParamUpdate.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MsgUpdateMarketParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.updates?.length) {
      obj.updates = message.updates.map((e) => MarketParamUpdate.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateMarketParams>, I>>(base?: I): MsgUpdateMarketParams {
    return MsgUpdateMarketParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateMarketParams>, I>>(object: I): MsgUpdateMarketParams {
    const message = createBaseMsgUpdateMarketParams();
    message.authority = object.authority ?? "";
    message.updates = object.updates?.map((e) => MarketParamUpdate.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgUpdateMarketParamsResponse(): MsgUpdateMarketParamsResponse {
  return {};
}

export const MsgUpdateMarketParamsResponse: MessageFns<MsgUpdateMarketParamsResponse> = {
  encode(_: MsgUpdateMarketParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateMarketParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateMarketParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateMarketParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateMarketParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateMarketParamsResponse>, I>>(base?: I): MsgUpdateMarketParamsResponse {
    return MsgUpdateMarketParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateMarketParamsResponse>, I>>(_: I): MsgUpdateMarketParamsResponse {
    const message = createBaseMsgUpdateMarketParamsResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
