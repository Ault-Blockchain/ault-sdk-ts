// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/license/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { License, Params, ProposalSnapshot } from "./license";

export const protobufPackage = "ault.license.v1";

/** GenesisState defines the license module's genesis state */
export interface GenesisState {
  /** params defines the module parameters */
  params:
    | Params
    | undefined;
  /** next_id is the next license ID to be assigned */
  nextId: bigint;
  /** licenses is the list of all licenses */
  licenses: License[];
  /** minters is the list of authorized minter addresses */
  minters: string[];
  /** supply is the total supply of active licenses (excluding burned) */
  supply: bigint;
  /** owner_counts maps owner addresses to their license counts */
  ownerCounts: OwnerCount[];
  /** owner_indices maps owner addresses to their license indices */
  ownerIndices: OwnerIndex[];
  /** active_count is the count of active licenses (for efficient import/export) */
  activeCount: bigint;
  /**
   * revoked_count is the count of revoked licenses (for efficient
   * import/export)
   */
  revokedCount: bigint;
  /**
   * genesis_time is the chain genesis time used for transfer lockup
   * calculations
   */
  genesisTime:
    | Date
    | undefined;
  /** kyc_approvers is the list of authorized KYC approver addresses */
  kycApprovers: string[];
  /** approved_members is the list of approved DAO member addresses */
  approvedMembers: string[];
  /**
   * total_capped is the sum of min(active_licenses_i, cap) for all approved
   * members
   */
  totalCapped: bigint;
  /** proposal_snapshots stores voting power state for active proposals */
  proposalSnapshots: ProposalSnapshotEntry[];
  /** vote_voting_powers stores cached voting power for votes */
  voteVotingPowers: VoteVotingPowerEntry[];
}

/** OwnerCount represents the count of licenses for an owner */
export interface OwnerCount {
  /** owner is the address */
  owner: string;
  /** count is the number of licenses owned */
  count: number;
}

/** OwnerIndex represents the license indices for an owner */
export interface OwnerIndex {
  /** owner is the address */
  owner: string;
  /** indices is the list of license IDs owned */
  indices: bigint[];
}

/** ProposalSnapshotEntry represents a proposal snapshot for genesis export */
export interface ProposalSnapshotEntry {
  /** proposal_id is the governance proposal ID */
  proposalId: bigint;
  /** snapshot is the voting power snapshot */
  snapshot: ProposalSnapshot | undefined;
}

/** VoteVotingPowerEntry represents a vote's voting power for genesis export */
export interface VoteVotingPowerEntry {
  /** proposal_id is the governance proposal ID */
  proposalId: bigint;
  /** voter is the voter address */
  voter: string;
  /** voting_power is the voter's capped voting power at snapshot time */
  votingPower: bigint;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    nextId: 0n,
    licenses: [],
    minters: [],
    supply: 0n,
    ownerCounts: [],
    ownerIndices: [],
    activeCount: 0n,
    revokedCount: 0n,
    genesisTime: undefined,
    kycApprovers: [],
    approvedMembers: [],
    totalCapped: 0n,
    proposalSnapshots: [],
    voteVotingPowers: [],
  };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    if (message.nextId !== 0n) {
      if (BigInt.asUintN(64, message.nextId) !== message.nextId) {
        throw new globalThis.Error("value provided for field message.nextId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.nextId);
    }
    for (const v of message.licenses) {
      License.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.minters) {
      writer.uint32(34).string(v!);
    }
    if (message.supply !== 0n) {
      if (BigInt.asUintN(64, message.supply) !== message.supply) {
        throw new globalThis.Error("value provided for field message.supply of type uint64 too large");
      }
      writer.uint32(40).uint64(message.supply);
    }
    for (const v of message.ownerCounts) {
      OwnerCount.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.ownerIndices) {
      OwnerIndex.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.activeCount !== 0n) {
      if (BigInt.asUintN(64, message.activeCount) !== message.activeCount) {
        throw new globalThis.Error("value provided for field message.activeCount of type uint64 too large");
      }
      writer.uint32(64).uint64(message.activeCount);
    }
    if (message.revokedCount !== 0n) {
      if (BigInt.asUintN(64, message.revokedCount) !== message.revokedCount) {
        throw new globalThis.Error("value provided for field message.revokedCount of type uint64 too large");
      }
      writer.uint32(72).uint64(message.revokedCount);
    }
    if (message.genesisTime !== undefined) {
      Timestamp.encode(toTimestamp(message.genesisTime), writer.uint32(82).fork()).join();
    }
    for (const v of message.kycApprovers) {
      writer.uint32(90).string(v!);
    }
    for (const v of message.approvedMembers) {
      writer.uint32(98).string(v!);
    }
    if (message.totalCapped !== 0n) {
      if (BigInt.asUintN(64, message.totalCapped) !== message.totalCapped) {
        throw new globalThis.Error("value provided for field message.totalCapped of type uint64 too large");
      }
      writer.uint32(104).uint64(message.totalCapped);
    }
    for (const v of message.proposalSnapshots) {
      ProposalSnapshotEntry.encode(v!, writer.uint32(114).fork()).join();
    }
    for (const v of message.voteVotingPowers) {
      VoteVotingPowerEntry.encode(v!, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextId = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.licenses.push(License.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.minters.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.supply = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ownerCounts.push(OwnerCount.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.ownerIndices.push(OwnerIndex.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.activeCount = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.revokedCount = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.genesisTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.kycApprovers.push(reader.string());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.approvedMembers.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.totalCapped = reader.uint64() as bigint;
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.proposalSnapshots.push(ProposalSnapshotEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.voteVotingPowers.push(VoteVotingPowerEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      nextId: isSet(object.nextId) ? BigInt(object.nextId) : isSet(object.next_id) ? BigInt(object.next_id) : 0n,
      licenses: globalThis.Array.isArray(object?.licenses) ? object.licenses.map((e: any) => License.fromJSON(e)) : [],
      minters: globalThis.Array.isArray(object?.minters) ? object.minters.map((e: any) => globalThis.String(e)) : [],
      supply: isSet(object.supply) ? BigInt(object.supply) : 0n,
      ownerCounts: globalThis.Array.isArray(object?.ownerCounts)
        ? object.ownerCounts.map((e: any) => OwnerCount.fromJSON(e))
        : globalThis.Array.isArray(object?.owner_counts)
        ? object.owner_counts.map((e: any) => OwnerCount.fromJSON(e))
        : [],
      ownerIndices: globalThis.Array.isArray(object?.ownerIndices)
        ? object.ownerIndices.map((e: any) => OwnerIndex.fromJSON(e))
        : globalThis.Array.isArray(object?.owner_indices)
        ? object.owner_indices.map((e: any) => OwnerIndex.fromJSON(e))
        : [],
      activeCount: isSet(object.activeCount)
        ? BigInt(object.activeCount)
        : isSet(object.active_count)
        ? BigInt(object.active_count)
        : 0n,
      revokedCount: isSet(object.revokedCount)
        ? BigInt(object.revokedCount)
        : isSet(object.revoked_count)
        ? BigInt(object.revoked_count)
        : 0n,
      genesisTime: isSet(object.genesisTime)
        ? fromJsonTimestamp(object.genesisTime)
        : isSet(object.genesis_time)
        ? fromJsonTimestamp(object.genesis_time)
        : undefined,
      kycApprovers: globalThis.Array.isArray(object?.kycApprovers)
        ? object.kycApprovers.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.kyc_approvers)
        ? object.kyc_approvers.map((e: any) => globalThis.String(e))
        : [],
      approvedMembers: globalThis.Array.isArray(object?.approvedMembers)
        ? object.approvedMembers.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.approved_members)
        ? object.approved_members.map((e: any) => globalThis.String(e))
        : [],
      totalCapped: isSet(object.totalCapped)
        ? BigInt(object.totalCapped)
        : isSet(object.total_capped)
        ? BigInt(object.total_capped)
        : 0n,
      proposalSnapshots: globalThis.Array.isArray(object?.proposalSnapshots)
        ? object.proposalSnapshots.map((e: any) => ProposalSnapshotEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.proposal_snapshots)
        ? object.proposal_snapshots.map((e: any) => ProposalSnapshotEntry.fromJSON(e))
        : [],
      voteVotingPowers: globalThis.Array.isArray(object?.voteVotingPowers)
        ? object.voteVotingPowers.map((e: any) => VoteVotingPowerEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.vote_voting_powers)
        ? object.vote_voting_powers.map((e: any) => VoteVotingPowerEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.nextId !== 0n) {
      obj.nextId = message.nextId.toString();
    }
    if (message.licenses?.length) {
      obj.licenses = message.licenses.map((e) => License.toJSON(e));
    }
    if (message.minters?.length) {
      obj.minters = message.minters;
    }
    if (message.supply !== 0n) {
      obj.supply = message.supply.toString();
    }
    if (message.ownerCounts?.length) {
      obj.ownerCounts = message.ownerCounts.map((e) => OwnerCount.toJSON(e));
    }
    if (message.ownerIndices?.length) {
      obj.ownerIndices = message.ownerIndices.map((e) => OwnerIndex.toJSON(e));
    }
    if (message.activeCount !== 0n) {
      obj.activeCount = message.activeCount.toString();
    }
    if (message.revokedCount !== 0n) {
      obj.revokedCount = message.revokedCount.toString();
    }
    if (message.genesisTime !== undefined) {
      obj.genesisTime = message.genesisTime.toISOString();
    }
    if (message.kycApprovers?.length) {
      obj.kycApprovers = message.kycApprovers;
    }
    if (message.approvedMembers?.length) {
      obj.approvedMembers = message.approvedMembers;
    }
    if (message.totalCapped !== 0n) {
      obj.totalCapped = message.totalCapped.toString();
    }
    if (message.proposalSnapshots?.length) {
      obj.proposalSnapshots = message.proposalSnapshots.map((e) => ProposalSnapshotEntry.toJSON(e));
    }
    if (message.voteVotingPowers?.length) {
      obj.voteVotingPowers = message.voteVotingPowers.map((e) => VoteVotingPowerEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.nextId = object.nextId ?? 0n;
    message.licenses = object.licenses?.map((e) => License.fromPartial(e)) || [];
    message.minters = object.minters?.map((e) => e) || [];
    message.supply = object.supply ?? 0n;
    message.ownerCounts = object.ownerCounts?.map((e) => OwnerCount.fromPartial(e)) || [];
    message.ownerIndices = object.ownerIndices?.map((e) => OwnerIndex.fromPartial(e)) || [];
    message.activeCount = object.activeCount ?? 0n;
    message.revokedCount = object.revokedCount ?? 0n;
    message.genesisTime = object.genesisTime ?? undefined;
    message.kycApprovers = object.kycApprovers?.map((e) => e) || [];
    message.approvedMembers = object.approvedMembers?.map((e) => e) || [];
    message.totalCapped = object.totalCapped ?? 0n;
    message.proposalSnapshots = object.proposalSnapshots?.map((e) => ProposalSnapshotEntry.fromPartial(e)) || [];
    message.voteVotingPowers = object.voteVotingPowers?.map((e) => VoteVotingPowerEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOwnerCount(): OwnerCount {
  return { owner: "", count: 0 };
}

export const OwnerCount: MessageFns<OwnerCount> = {
  encode(message: OwnerCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.count !== 0) {
      writer.uint32(16).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OwnerCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOwnerCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OwnerCount {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: OwnerCount): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OwnerCount>, I>>(base?: I): OwnerCount {
    return OwnerCount.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OwnerCount>, I>>(object: I): OwnerCount {
    const message = createBaseOwnerCount();
    message.owner = object.owner ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseOwnerIndex(): OwnerIndex {
  return { owner: "", indices: [] };
}

export const OwnerIndex: MessageFns<OwnerIndex> = {
  encode(message: OwnerIndex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    writer.uint32(18).fork();
    for (const v of message.indices) {
      if (BigInt.asUintN(64, v) !== v) {
        throw new globalThis.Error("a value provided in array field indices of type uint64 is too large");
      }
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OwnerIndex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOwnerIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.indices.push(reader.uint64() as bigint);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.indices.push(reader.uint64() as bigint);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OwnerIndex {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      indices: globalThis.Array.isArray(object?.indices) ? object.indices.map((e: any) => BigInt(e)) : [],
    };
  },

  toJSON(message: OwnerIndex): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.indices?.length) {
      obj.indices = message.indices.map((e) => e.toString());
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OwnerIndex>, I>>(base?: I): OwnerIndex {
    return OwnerIndex.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OwnerIndex>, I>>(object: I): OwnerIndex {
    const message = createBaseOwnerIndex();
    message.owner = object.owner ?? "";
    message.indices = object.indices?.map((e) => e) || [];
    return message;
  },
};

function createBaseProposalSnapshotEntry(): ProposalSnapshotEntry {
  return { proposalId: 0n, snapshot: undefined };
}

export const ProposalSnapshotEntry: MessageFns<ProposalSnapshotEntry> = {
  encode(message: ProposalSnapshotEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proposalId !== 0n) {
      if (BigInt.asUintN(64, message.proposalId) !== message.proposalId) {
        throw new globalThis.Error("value provided for field message.proposalId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.proposalId);
    }
    if (message.snapshot !== undefined) {
      ProposalSnapshot.encode(message.snapshot, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProposalSnapshotEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalSnapshotEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.proposalId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.snapshot = ProposalSnapshot.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalSnapshotEntry {
    return {
      proposalId: isSet(object.proposalId)
        ? BigInt(object.proposalId)
        : isSet(object.proposal_id)
        ? BigInt(object.proposal_id)
        : 0n,
      snapshot: isSet(object.snapshot) ? ProposalSnapshot.fromJSON(object.snapshot) : undefined,
    };
  },

  toJSON(message: ProposalSnapshotEntry): unknown {
    const obj: any = {};
    if (message.proposalId !== 0n) {
      obj.proposalId = message.proposalId.toString();
    }
    if (message.snapshot !== undefined) {
      obj.snapshot = ProposalSnapshot.toJSON(message.snapshot);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalSnapshotEntry>, I>>(base?: I): ProposalSnapshotEntry {
    return ProposalSnapshotEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalSnapshotEntry>, I>>(object: I): ProposalSnapshotEntry {
    const message = createBaseProposalSnapshotEntry();
    message.proposalId = object.proposalId ?? 0n;
    message.snapshot = (object.snapshot !== undefined && object.snapshot !== null)
      ? ProposalSnapshot.fromPartial(object.snapshot)
      : undefined;
    return message;
  },
};

function createBaseVoteVotingPowerEntry(): VoteVotingPowerEntry {
  return { proposalId: 0n, voter: "", votingPower: 0n };
}

export const VoteVotingPowerEntry: MessageFns<VoteVotingPowerEntry> = {
  encode(message: VoteVotingPowerEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proposalId !== 0n) {
      if (BigInt.asUintN(64, message.proposalId) !== message.proposalId) {
        throw new globalThis.Error("value provided for field message.proposalId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.proposalId);
    }
    if (message.voter !== "") {
      writer.uint32(18).string(message.voter);
    }
    if (message.votingPower !== 0n) {
      if (BigInt.asUintN(64, message.votingPower) !== message.votingPower) {
        throw new globalThis.Error("value provided for field message.votingPower of type uint64 too large");
      }
      writer.uint32(24).uint64(message.votingPower);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteVotingPowerEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteVotingPowerEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.proposalId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.voter = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.votingPower = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoteVotingPowerEntry {
    return {
      proposalId: isSet(object.proposalId)
        ? BigInt(object.proposalId)
        : isSet(object.proposal_id)
        ? BigInt(object.proposal_id)
        : 0n,
      voter: isSet(object.voter) ? globalThis.String(object.voter) : "",
      votingPower: isSet(object.votingPower)
        ? BigInt(object.votingPower)
        : isSet(object.voting_power)
        ? BigInt(object.voting_power)
        : 0n,
    };
  },

  toJSON(message: VoteVotingPowerEntry): unknown {
    const obj: any = {};
    if (message.proposalId !== 0n) {
      obj.proposalId = message.proposalId.toString();
    }
    if (message.voter !== "") {
      obj.voter = message.voter;
    }
    if (message.votingPower !== 0n) {
      obj.votingPower = message.votingPower.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteVotingPowerEntry>, I>>(base?: I): VoteVotingPowerEntry {
    return VoteVotingPowerEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteVotingPowerEntry>, I>>(object: I): VoteVotingPowerEntry {
    const message = createBaseVoteVotingPowerEntry();
    message.proposalId = object.proposalId ?? 0n;
    message.voter = object.voter ?? "";
    message.votingPower = object.votingPower ?? 0n;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
