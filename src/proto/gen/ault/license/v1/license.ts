// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: ault/license/v1/license.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "ault.license.v1";

/** LicenseStatus defines the status of a license */
export enum LicenseStatus {
  /** LICENSE_STATUS_UNSPECIFIED - LICENSE_STATUS_UNSPECIFIED is the default value */
  LICENSE_STATUS_UNSPECIFIED = 0,
  /** LICENSE_STATUS_ACTIVE - LICENSE_STATUS_ACTIVE indicates the license is active and can be used */
  LICENSE_STATUS_ACTIVE = 1,
  /** LICENSE_STATUS_REVOKED - LICENSE_STATUS_REVOKED indicates the license has been revoked */
  LICENSE_STATUS_REVOKED = 2,
  /** LICENSE_STATUS_BURNED - LICENSE_STATUS_BURNED indicates the license has been burned */
  LICENSE_STATUS_BURNED = 3,
  UNRECOGNIZED = -1,
}

export function licenseStatusFromJSON(object: any): LicenseStatus {
  switch (object) {
    case 0:
    case "LICENSE_STATUS_UNSPECIFIED":
      return LicenseStatus.LICENSE_STATUS_UNSPECIFIED;
    case 1:
    case "LICENSE_STATUS_ACTIVE":
      return LicenseStatus.LICENSE_STATUS_ACTIVE;
    case 2:
    case "LICENSE_STATUS_REVOKED":
      return LicenseStatus.LICENSE_STATUS_REVOKED;
    case 3:
    case "LICENSE_STATUS_BURNED":
      return LicenseStatus.LICENSE_STATUS_BURNED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LicenseStatus.UNRECOGNIZED;
  }
}

export function licenseStatusToJSON(object: LicenseStatus): string {
  switch (object) {
    case LicenseStatus.LICENSE_STATUS_UNSPECIFIED:
      return "LICENSE_STATUS_UNSPECIFIED";
    case LicenseStatus.LICENSE_STATUS_ACTIVE:
      return "LICENSE_STATUS_ACTIVE";
    case LicenseStatus.LICENSE_STATUS_REVOKED:
      return "LICENSE_STATUS_REVOKED";
    case LicenseStatus.LICENSE_STATUS_BURNED:
      return "LICENSE_STATUS_BURNED";
    case LicenseStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** License defines a soul-bound token license */
export interface License {
  /** id is the unique identifier of the license */
  id: bigint;
  /** owner is the address that owns this license */
  owner: string;
  /** status is the current status of the license */
  status: LicenseStatus;
  /** class_name is the class name of the license (for ERC-721 compatibility) */
  className: string;
  /** uri is the metadata URI for this license */
  uri: string;
  /** created_at is the timestamp when the license was created (minted). */
  createdAt:
    | Date
    | undefined;
  /**
   * revoked_at is the timestamp when the license was revoked.
   * Used for snapshot-based governance voting power calculation.
   * Nil means the license has never been revoked.
   */
  revokedAt:
    | Date
    | undefined;
  /**
   * burned_at is the timestamp when the license was burned.
   * Nil means the license has not been burned.
   */
  burnedAt: Date | undefined;
}

/** Params defines the parameters for the license module */
export interface Params {
  /** class_name is the name of the license class (ERC-721 name) */
  className: string;
  /** class_symbol is the symbol of the license class (ERC-721 symbol) */
  classSymbol: string;
  /** base_token_uri is the base URI prepended to token URIs */
  baseTokenUri: string;
  /** minting_paused indicates whether minting is currently paused */
  mintingPaused: boolean;
  /**
   * supply_cap is the maximum number of licenses that can be minted (0 =
   * unlimited)
   */
  supplyCap: bigint;
  /** allow_metadata_update indicates whether token URIs can be updated */
  allowMetadataUpdate: boolean;
  /** admin_can_revoke indicates whether admins can revoke licenses */
  adminCanRevoke: boolean;
  /** admin_can_burn indicates whether admins can burn licenses */
  adminCanBurn: boolean;
  /**
   * max_batch_size is the maximum number of items in a single batch operation
   * (license minting, member approval/revocation, etc.) Default is 1000.
   */
  maxBatchSize: number;
  /**
   * transfer_unlock_days is the number of days from genesis time
   * when all licenses become transferable globally
   */
  transferUnlockDays: number;
  /**
   * enable_transfers allows governance to globally enable/disable transfers
   * This is a kill switch in case of emergency
   */
  enableTransfers: boolean;
  /**
   * minter_allowed_msgs is the list of message type URLs that minters are
   * allowed to execute with free gas
   */
  minterAllowedMsgs: string[];
  /**
   * kyc_approver_allowed_msgs is the list of message type URLs that KYC
   * approvers are allowed to execute with free gas
   */
  kycApproverAllowedMsgs: string[];
  /**
   * free_max_gas_limit is the maximum gas allowed per transaction for free gas
   * users. Prevents abuse even with free gas.
   */
  freeMaxGasLimit: bigint;
  /**
   * max_voting_power_per_address is the maximum voting power per address in
   * governance. 0 means no cap. Used to prevent concentration of voting power.
   */
  maxVotingPowerPerAddress: bigint;
}

/** TransferRecord records a license transfer for snapshot-based governance */
export interface TransferRecord {
  /** license_id is the ID of the transferred license */
  licenseId: bigint;
  /** timestamp is when the transfer occurred */
  timestamp:
    | Date
    | undefined;
  /** from_owner is the previous owner */
  fromOwner: string;
  /** to_owner is the new owner */
  toOwner: string;
}

/** ProposalSnapshot stores voting power state captured at proposal creation */
export interface ProposalSnapshot {
  /** timestamp is when the snapshot was taken (proposal submission time) */
  timestamp:
    | Date
    | undefined;
  /**
   * total_capped is the sum of min(active_licenses_i, cap) for all approved
   * members at snapshot time
   */
  totalCapped: bigint;
  /** cap is the MaxVotingPowerPerAddress parameter value at snapshot time */
  cap: bigint;
}

function createBaseLicense(): License {
  return {
    id: 0n,
    owner: "",
    status: 0,
    className: "",
    uri: "",
    createdAt: undefined,
    revokedAt: undefined,
    burnedAt: undefined,
  };
}

export const License: MessageFns<License> = {
  encode(message: License, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.className !== "") {
      writer.uint32(34).string(message.className);
    }
    if (message.uri !== "") {
      writer.uint32(42).string(message.uri);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    if (message.revokedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.revokedAt), writer.uint32(58).fork()).join();
    }
    if (message.burnedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.burnedAt), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): License {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLicense();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.className = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.revokedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.burnedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): License {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      status: isSet(object.status) ? licenseStatusFromJSON(object.status) : 0,
      className: isSet(object.className)
        ? globalThis.String(object.className)
        : isSet(object.class_name)
        ? globalThis.String(object.class_name)
        : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      revokedAt: isSet(object.revokedAt)
        ? fromJsonTimestamp(object.revokedAt)
        : isSet(object.revoked_at)
        ? fromJsonTimestamp(object.revoked_at)
        : undefined,
      burnedAt: isSet(object.burnedAt)
        ? fromJsonTimestamp(object.burnedAt)
        : isSet(object.burned_at)
        ? fromJsonTimestamp(object.burned_at)
        : undefined,
    };
  },

  toJSON(message: License): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.status !== 0) {
      obj.status = licenseStatusToJSON(message.status);
    }
    if (message.className !== "") {
      obj.className = message.className;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.revokedAt !== undefined) {
      obj.revokedAt = message.revokedAt.toISOString();
    }
    if (message.burnedAt !== undefined) {
      obj.burnedAt = message.burnedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<License>, I>>(base?: I): License {
    return License.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<License>, I>>(object: I): License {
    const message = createBaseLicense();
    message.id = object.id ?? 0n;
    message.owner = object.owner ?? "";
    message.status = object.status ?? 0;
    message.className = object.className ?? "";
    message.uri = object.uri ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.revokedAt = object.revokedAt ?? undefined;
    message.burnedAt = object.burnedAt ?? undefined;
    return message;
  },
};

function createBaseParams(): Params {
  return {
    className: "",
    classSymbol: "",
    baseTokenUri: "",
    mintingPaused: false,
    supplyCap: 0n,
    allowMetadataUpdate: false,
    adminCanRevoke: false,
    adminCanBurn: false,
    maxBatchSize: 0,
    transferUnlockDays: 0,
    enableTransfers: false,
    minterAllowedMsgs: [],
    kycApproverAllowedMsgs: [],
    freeMaxGasLimit: 0n,
    maxVotingPowerPerAddress: 0n,
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.className !== "") {
      writer.uint32(10).string(message.className);
    }
    if (message.classSymbol !== "") {
      writer.uint32(18).string(message.classSymbol);
    }
    if (message.baseTokenUri !== "") {
      writer.uint32(26).string(message.baseTokenUri);
    }
    if (message.mintingPaused !== false) {
      writer.uint32(32).bool(message.mintingPaused);
    }
    if (message.supplyCap !== 0n) {
      if (BigInt.asUintN(64, message.supplyCap) !== message.supplyCap) {
        throw new globalThis.Error("value provided for field message.supplyCap of type uint64 too large");
      }
      writer.uint32(40).uint64(message.supplyCap);
    }
    if (message.allowMetadataUpdate !== false) {
      writer.uint32(48).bool(message.allowMetadataUpdate);
    }
    if (message.adminCanRevoke !== false) {
      writer.uint32(56).bool(message.adminCanRevoke);
    }
    if (message.adminCanBurn !== false) {
      writer.uint32(64).bool(message.adminCanBurn);
    }
    if (message.maxBatchSize !== 0) {
      writer.uint32(72).uint32(message.maxBatchSize);
    }
    if (message.transferUnlockDays !== 0) {
      writer.uint32(80).uint32(message.transferUnlockDays);
    }
    if (message.enableTransfers !== false) {
      writer.uint32(88).bool(message.enableTransfers);
    }
    for (const v of message.minterAllowedMsgs) {
      writer.uint32(98).string(v!);
    }
    for (const v of message.kycApproverAllowedMsgs) {
      writer.uint32(106).string(v!);
    }
    if (message.freeMaxGasLimit !== 0n) {
      if (BigInt.asUintN(64, message.freeMaxGasLimit) !== message.freeMaxGasLimit) {
        throw new globalThis.Error("value provided for field message.freeMaxGasLimit of type uint64 too large");
      }
      writer.uint32(112).uint64(message.freeMaxGasLimit);
    }
    if (message.maxVotingPowerPerAddress !== 0n) {
      if (BigInt.asUintN(64, message.maxVotingPowerPerAddress) !== message.maxVotingPowerPerAddress) {
        throw new globalThis.Error(
          "value provided for field message.maxVotingPowerPerAddress of type uint64 too large",
        );
      }
      writer.uint32(120).uint64(message.maxVotingPowerPerAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.className = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.classSymbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseTokenUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.mintingPaused = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.supplyCap = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allowMetadataUpdate = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.adminCanRevoke = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.adminCanBurn = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.maxBatchSize = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.transferUnlockDays = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.enableTransfers = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.minterAllowedMsgs.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.kycApproverAllowedMsgs.push(reader.string());
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.freeMaxGasLimit = reader.uint64() as bigint;
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.maxVotingPowerPerAddress = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      className: isSet(object.className)
        ? globalThis.String(object.className)
        : isSet(object.class_name)
        ? globalThis.String(object.class_name)
        : "",
      classSymbol: isSet(object.classSymbol)
        ? globalThis.String(object.classSymbol)
        : isSet(object.class_symbol)
        ? globalThis.String(object.class_symbol)
        : "",
      baseTokenUri: isSet(object.baseTokenUri)
        ? globalThis.String(object.baseTokenUri)
        : isSet(object.base_token_uri)
        ? globalThis.String(object.base_token_uri)
        : "",
      mintingPaused: isSet(object.mintingPaused)
        ? globalThis.Boolean(object.mintingPaused)
        : isSet(object.minting_paused)
        ? globalThis.Boolean(object.minting_paused)
        : false,
      supplyCap: isSet(object.supplyCap)
        ? BigInt(object.supplyCap)
        : isSet(object.supply_cap)
        ? BigInt(object.supply_cap)
        : 0n,
      allowMetadataUpdate: isSet(object.allowMetadataUpdate)
        ? globalThis.Boolean(object.allowMetadataUpdate)
        : isSet(object.allow_metadata_update)
        ? globalThis.Boolean(object.allow_metadata_update)
        : false,
      adminCanRevoke: isSet(object.adminCanRevoke)
        ? globalThis.Boolean(object.adminCanRevoke)
        : isSet(object.admin_can_revoke)
        ? globalThis.Boolean(object.admin_can_revoke)
        : false,
      adminCanBurn: isSet(object.adminCanBurn)
        ? globalThis.Boolean(object.adminCanBurn)
        : isSet(object.admin_can_burn)
        ? globalThis.Boolean(object.admin_can_burn)
        : false,
      maxBatchSize: isSet(object.maxBatchSize)
        ? globalThis.Number(object.maxBatchSize)
        : isSet(object.max_batch_size)
        ? globalThis.Number(object.max_batch_size)
        : 0,
      transferUnlockDays: isSet(object.transferUnlockDays)
        ? globalThis.Number(object.transferUnlockDays)
        : isSet(object.transfer_unlock_days)
        ? globalThis.Number(object.transfer_unlock_days)
        : 0,
      enableTransfers: isSet(object.enableTransfers)
        ? globalThis.Boolean(object.enableTransfers)
        : isSet(object.enable_transfers)
        ? globalThis.Boolean(object.enable_transfers)
        : false,
      minterAllowedMsgs: globalThis.Array.isArray(object?.minterAllowedMsgs)
        ? object.minterAllowedMsgs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.minter_allowed_msgs)
        ? object.minter_allowed_msgs.map((e: any) => globalThis.String(e))
        : [],
      kycApproverAllowedMsgs: globalThis.Array.isArray(object?.kycApproverAllowedMsgs)
        ? object.kycApproverAllowedMsgs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.kyc_approver_allowed_msgs)
        ? object.kyc_approver_allowed_msgs.map((e: any) => globalThis.String(e))
        : [],
      freeMaxGasLimit: isSet(object.freeMaxGasLimit)
        ? BigInt(object.freeMaxGasLimit)
        : isSet(object.free_max_gas_limit)
        ? BigInt(object.free_max_gas_limit)
        : 0n,
      maxVotingPowerPerAddress: isSet(object.maxVotingPowerPerAddress)
        ? BigInt(object.maxVotingPowerPerAddress)
        : isSet(object.max_voting_power_per_address)
        ? BigInt(object.max_voting_power_per_address)
        : 0n,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.className !== "") {
      obj.className = message.className;
    }
    if (message.classSymbol !== "") {
      obj.classSymbol = message.classSymbol;
    }
    if (message.baseTokenUri !== "") {
      obj.baseTokenUri = message.baseTokenUri;
    }
    if (message.mintingPaused !== false) {
      obj.mintingPaused = message.mintingPaused;
    }
    if (message.supplyCap !== 0n) {
      obj.supplyCap = message.supplyCap.toString();
    }
    if (message.allowMetadataUpdate !== false) {
      obj.allowMetadataUpdate = message.allowMetadataUpdate;
    }
    if (message.adminCanRevoke !== false) {
      obj.adminCanRevoke = message.adminCanRevoke;
    }
    if (message.adminCanBurn !== false) {
      obj.adminCanBurn = message.adminCanBurn;
    }
    if (message.maxBatchSize !== 0) {
      obj.maxBatchSize = Math.round(message.maxBatchSize);
    }
    if (message.transferUnlockDays !== 0) {
      obj.transferUnlockDays = Math.round(message.transferUnlockDays);
    }
    if (message.enableTransfers !== false) {
      obj.enableTransfers = message.enableTransfers;
    }
    if (message.minterAllowedMsgs?.length) {
      obj.minterAllowedMsgs = message.minterAllowedMsgs;
    }
    if (message.kycApproverAllowedMsgs?.length) {
      obj.kycApproverAllowedMsgs = message.kycApproverAllowedMsgs;
    }
    if (message.freeMaxGasLimit !== 0n) {
      obj.freeMaxGasLimit = message.freeMaxGasLimit.toString();
    }
    if (message.maxVotingPowerPerAddress !== 0n) {
      obj.maxVotingPowerPerAddress = message.maxVotingPowerPerAddress.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.className = object.className ?? "";
    message.classSymbol = object.classSymbol ?? "";
    message.baseTokenUri = object.baseTokenUri ?? "";
    message.mintingPaused = object.mintingPaused ?? false;
    message.supplyCap = object.supplyCap ?? 0n;
    message.allowMetadataUpdate = object.allowMetadataUpdate ?? false;
    message.adminCanRevoke = object.adminCanRevoke ?? false;
    message.adminCanBurn = object.adminCanBurn ?? false;
    message.maxBatchSize = object.maxBatchSize ?? 0;
    message.transferUnlockDays = object.transferUnlockDays ?? 0;
    message.enableTransfers = object.enableTransfers ?? false;
    message.minterAllowedMsgs = object.minterAllowedMsgs?.map((e) => e) || [];
    message.kycApproverAllowedMsgs = object.kycApproverAllowedMsgs?.map((e) => e) || [];
    message.freeMaxGasLimit = object.freeMaxGasLimit ?? 0n;
    message.maxVotingPowerPerAddress = object.maxVotingPowerPerAddress ?? 0n;
    return message;
  },
};

function createBaseTransferRecord(): TransferRecord {
  return { licenseId: 0n, timestamp: undefined, fromOwner: "", toOwner: "" };
}

export const TransferRecord: MessageFns<TransferRecord> = {
  encode(message: TransferRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.licenseId !== 0n) {
      if (BigInt.asUintN(64, message.licenseId) !== message.licenseId) {
        throw new globalThis.Error("value provided for field message.licenseId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.licenseId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    if (message.fromOwner !== "") {
      writer.uint32(26).string(message.fromOwner);
    }
    if (message.toOwner !== "") {
      writer.uint32(34).string(message.toOwner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransferRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransferRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.licenseId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fromOwner = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toOwner = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransferRecord {
    return {
      licenseId: isSet(object.licenseId)
        ? BigInt(object.licenseId)
        : isSet(object.license_id)
        ? BigInt(object.license_id)
        : 0n,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      fromOwner: isSet(object.fromOwner)
        ? globalThis.String(object.fromOwner)
        : isSet(object.from_owner)
        ? globalThis.String(object.from_owner)
        : "",
      toOwner: isSet(object.toOwner)
        ? globalThis.String(object.toOwner)
        : isSet(object.to_owner)
        ? globalThis.String(object.to_owner)
        : "",
    };
  },

  toJSON(message: TransferRecord): unknown {
    const obj: any = {};
    if (message.licenseId !== 0n) {
      obj.licenseId = message.licenseId.toString();
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.fromOwner !== "") {
      obj.fromOwner = message.fromOwner;
    }
    if (message.toOwner !== "") {
      obj.toOwner = message.toOwner;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransferRecord>, I>>(base?: I): TransferRecord {
    return TransferRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransferRecord>, I>>(object: I): TransferRecord {
    const message = createBaseTransferRecord();
    message.licenseId = object.licenseId ?? 0n;
    message.timestamp = object.timestamp ?? undefined;
    message.fromOwner = object.fromOwner ?? "";
    message.toOwner = object.toOwner ?? "";
    return message;
  },
};

function createBaseProposalSnapshot(): ProposalSnapshot {
  return { timestamp: undefined, totalCapped: 0n, cap: 0n };
}

export const ProposalSnapshot: MessageFns<ProposalSnapshot> = {
  encode(message: ProposalSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).join();
    }
    if (message.totalCapped !== 0n) {
      if (BigInt.asUintN(64, message.totalCapped) !== message.totalCapped) {
        throw new globalThis.Error("value provided for field message.totalCapped of type uint64 too large");
      }
      writer.uint32(16).uint64(message.totalCapped);
    }
    if (message.cap !== 0n) {
      if (BigInt.asUintN(64, message.cap) !== message.cap) {
        throw new globalThis.Error("value provided for field message.cap of type uint64 too large");
      }
      writer.uint32(24).uint64(message.cap);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProposalSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposalSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCapped = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.cap = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposalSnapshot {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      totalCapped: isSet(object.totalCapped)
        ? BigInt(object.totalCapped)
        : isSet(object.total_capped)
        ? BigInt(object.total_capped)
        : 0n,
      cap: isSet(object.cap) ? BigInt(object.cap) : 0n,
    };
  },

  toJSON(message: ProposalSnapshot): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.totalCapped !== 0n) {
      obj.totalCapped = message.totalCapped.toString();
    }
    if (message.cap !== 0n) {
      obj.cap = message.cap.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposalSnapshot>, I>>(base?: I): ProposalSnapshot {
    return ProposalSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposalSnapshot>, I>>(object: I): ProposalSnapshot {
    const message = createBaseProposalSnapshot();
    message.timestamp = object.timestamp ?? undefined;
    message.totalCapped = object.totalCapped ?? 0n;
    message.cap = object.cap ?? 0n;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
